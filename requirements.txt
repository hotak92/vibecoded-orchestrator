# VibeCoded Tools — Orchestrator Dependencies
# All licenses verified: MIT, Apache 2.0, or BSD-3-Clause

# ─── MCP SDK ───
# MCP Python SDK (MIT) — required for weaviate-kg, ollama, and search MCP
# servers. All three import the bundled FastMCP submodule of the official
# Anthropic SDK via `from mcp.server.fastmcp import FastMCP`.
# (The standalone PyPI `fastmcp` package is NOT a dependency — its only
# call site, an unused `ToolResult` import in weaviate_mcp/server.py, was
# removed on 2026-04-28. See PR #31 for context.)
mcp>=1.27.0

# ─── Weaviate ───
# Weaviate Python client (BSD-3-Clause)
weaviate-client>=4.9

# ─── HTTP / async ───
aiohttp>=3.9
httpx>=0.27
requests>=2.31

# ─── Data validation ───
# Pydantic (MIT)
pydantic>=2.13.3

# ─── YAML parsing ───
# PyYAML (MIT)
pyyaml>=6.0.3

# ─── File watching ───
# Watchdog (Apache 2.0)
watchdog>=4.0

# ─── Database ───
# asyncpg (Apache 2.0) — optional, for persistent state
asyncpg>=0.29

# ─── Ollama client ───
# Ollama Python client (MIT) — for local LLM inference MCP
ollama>=0.6.1

# ─── Code embedding service (runs in container, but needed for CPU fallback) ───
# sentence-transformers (Apache 2.0)
sentence-transformers>=3.0
# transformers pinned for CodeSage Conv1D compatibility
transformers>=4.40.0,<5.0.0
# PyTorch (BSD-3-Clause) — for RL pipeline (Pro) and code embeddings (CPU fallback)
# NOTE: torch>=2.11 ships CUDA 13.0 wheels by default on PyPI. Users with
# CUDA 12.x drivers, Volta/Pascal/Maxwell GPUs, or CPU-only systems should
# pre-install a compatible torch wheel:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
# before running `pip install -r requirements.txt`. See KNOWN_ISSUES.md.
torch>=2.11.0

# ─── FastAPI (code embedding service HTTP server) ───
# FastAPI (MIT) + uvicorn (BSD-3-Clause)
fastapi>=0.111
uvicorn>=0.30

# ─── Telemetry (optional runtime) ───
# psutil (BSD-3-Clause) — used by VCThelpers.telemetry for opt-in hardware
# profiling. Already a transitive dep of several packages; pinned here so
# the telemetry module can import it directly.
psutil>=5.9
