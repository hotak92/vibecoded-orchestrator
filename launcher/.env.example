# Supabase Auth
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIs...

# NOTE: license validation is handled by the Rust backend via the Supabase
# `validate-tier` edge function. No payment-provider API key belongs in this
# file — anything prefixed VITE_ is embedded into the client bundle.
