{
  "_comment": "Cross-language parity fixture for the .claude/env managed-block splice. Consumed by BOTH tests/test_v0297_managed_block_merge_parity.py (Python vco_lib.config_projection._merge_managed_block — the writer apply uses) AND the Rust #[test] managed_block_merge_matches_the_shared_parity_fixture in launcher/src-tauri/src/commands/projects_v2.rs (merge_claude_env_managed_block — the one deliberate Rust mirror, used by the launcher's unregister strip). `expected` is what BOTH must return. MUST MATCH between Python + Rust.",
  "_format_version": 1,
  "begin_marker": "# vco-managed-begin",
  "end_marker": "# vco-managed-end",
  "cases": [
    {
      "name": "no_prior_file",
      "prior": null,
      "managed": "# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n",
      "expected": "# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n"
    },
    {
      "name": "legacy_file_without_markers_trailing_newline",
      "prior": "export OLD=\"1\"\n",
      "managed": "# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n",
      "expected": "export OLD=\"1\"\n# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n"
    },
    {
      "name": "legacy_file_without_markers_no_trailing_newline",
      "prior": "export OLD=\"1\"",
      "managed": "# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n",
      "expected": "export OLD=\"1\"\n# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n"
    },
    {
      "name": "empty_prior",
      "prior": "",
      "managed": "# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n",
      "expected": "\n# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n"
    },
    {
      "name": "replace_in_place_keeping_user_lines",
      "prior": "# mine\nexport MY=\"a\"\n# vco-managed-begin\nexport KG_COLLECTION=\"Old\"\n# vco-managed-end\n# trailer\n",
      "managed": "# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n",
      "expected": "# mine\nexport MY=\"a\"\n# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n# trailer\n"
    },
    {
      "name": "end_at_eof_without_newline",
      "prior": "export MY=\"a\"\n# vco-managed-begin\nexport X=\"1\"\n# vco-managed-end",
      "managed": "# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n",
      "expected": "export MY=\"a\"\n# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n"
    },
    {
      "name": "truncated_block_without_end",
      "prior": "export MY=\"a\"\n# vco-managed-begin\nexport X=\"1\"\n",
      "managed": "# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n",
      "expected": "export MY=\"a\"\n# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n"
    },
    {
      "name": "strip_block_with_empty_managed_unregister",
      "prior": "# mine\n# vco-managed-begin\nexport KG_COLLECTION=\"Old\"\n# vco-managed-end\nexport AFTER=\"z\"\n",
      "managed": "",
      "expected": "# mine\nexport AFTER=\"z\"\n"
    },
    {
      "name": "stray_end_before_begin",
      "prior": "# vco-managed-end\n# note\n# vco-managed-begin\nexport X=\"1\"\n# vco-managed-end\n",
      "managed": "# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n",
      "expected": "# vco-managed-end\n# note\n# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n"
    },
    {
      "name": "crlf_file",
      "prior": "export MY=\"a\"\r\n# vco-managed-begin\r\nexport X=\"1\"\r\n# vco-managed-end\r\n",
      "managed": "# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n",
      "expected": "export MY=\"a\"\r\n# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"New_KnowledgeGraph\"\nexport VCT_PROJECT_ID=\"p-1\"\n# vco-managed-end\n\r\n"
    },
    {
      "name": "unicode_values",
      "prior": "export NOTE=\"caffè ☕\"\n# vco-managed-begin\nexport X=\"1\"\n# vco-managed-end\n",
      "managed": "# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"Café_KnowledgeGraph\"\n# vco-managed-end\n",
      "expected": "export NOTE=\"caffè ☕\"\n# vco-managed-begin\n# Auto-generated by VCT Launcher. Source from your shell rc or use\n# tools/claude wrapper (which auto-sources this file before exec'ing\n# the real claude binary). Lines OUTSIDE this BEGIN/END block are\n# preserved across re-runs — add custom exports there.\n# Asymmetric shared-KG access (2026-05-01): reads always-on; this\n# gates WRITES only. SHARED_KG_OPT_OUT is the legacy alias kept\n# for ~3 releases (target removal: 2026-08).\n# Portability keys VCT_ORCHESTRATOR_ROOT / VCT_INFRASTRUCTURE_DIR\n# (when present) point at the orchestrator clone + its infrastructure/\n# dir; consumed by .claude/hooks/ensure-containers.sh and the\n# bundled Python scripts that need the claude_mcp_servers/ package.\nexport KG_COLLECTION=\"Café_KnowledgeGraph\"\n# vco-managed-end\n"
    }
  ]
}
