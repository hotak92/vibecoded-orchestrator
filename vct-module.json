{
  "_canonical_counts_2026_05_16": "Ground truth verified on 2026-05-16 against main HEAD post-v0.2.11 PR-14 + v0.2.12 PR-39. Bundled agents: 29 (templates/agents/free/*.md, all free). Skills: 28 (templates/skills/*/, each with SKILL.md). Hooks: 20 (templates/hooks/*.sh \u2014 rendered into .claude/hooks/ at install time per PR-39 v0.2.12; source-of-truth lives in templates/). MCP servers: 4 \u2014 3 Python-stack containerized (claude_mcp_servers/{weaviate_mcp,search_mcp,code_embedding_service}) plus 1 system-managed Playwright MCP (npx -y @playwright/mcp@latest, registered by install.py during first-install; opt out with VCT_SKIP_PLAYWRIGHT=1). v0.2.11 MCP simplification: dropped claude_mcp_servers/ollama_mcp/ entirely (chat/read_document/read_image redundant with Claude's native capabilities); narrowed search_mcp to only search_papers (web_search, search_code, fetch_page also redundant with native WebSearch/WebFetch). Ollama remains as embedding INFRASTRUCTURE (qwen3-embedding via container) consumed by weaviate_mcp + code_embedding_service. README + CHANGELOG + this file MUST stay in sync with these. Version tracking: this `version` field must be bumped alongside Cargo.toml + tauri.conf.json + package.json on every release (the release-bump checklist in CLAUDE.md lists all four).",
  "schema_version": "1",
  "id": "orchestrator",
  "name": "VibeCoded Orchestrator",
  "version": "0.2.18",
  "description": "The core: 20 hooks, 29 bundled agents, 28 skills, KG + Code Graph, 4 MCP servers (3 containerized + Playwright via npx). Runs locally in Podman/Docker.",
  "license": {
    "spdx": "AGPL-3.0-or-later",
    "tier": "free",
    "required": false,
    "variant_ids": [],
    "min_orchestrator_tier": "free"
  },
  "components": [
    {
      "id": "knowledge-graph",
      "name": "Knowledge Graph",
      "path": "claude_mcp_servers/weaviate_mcp",
      "description": "Markdown-based KG with typed WikiLinks, Weaviate-backed embeddings, hook-driven auto-sync."
    },
    {
      "id": "code-graph",
      "name": "Code Graph",
      "path": "claude_mcp_servers/code_embedding_service",
      "description": "AST analysis via Tree-sitter for 10+ languages. Optional Joern integration for CFG/PDG."
    }
  ],
  "_bundled_secrets_doc": "0.2.0 fork-readiness sweep (item H1, 2026-05-08): the orchestrator core declares the secrets the launcher itself manages (PAT for GitHub auth, etc.) so the hub's `/api/v1/projects/{id}/env` resolver can serve them to bundled wrappers without the user having to install a separate module first. Each entry maps a single keychain slot \u2014 the hub uses the SENTINEL_SHARED project_id for shared scope (so all base-host projects see the same value) and the OnboardingWizard's `register_github_pat` writes to the matching slot. 2026-05-10 (post-0.2.0 backlog #6): module_id changed from 'installer' to 'user' so the wizard's writer matches the SecretsPanel 'Shared (this user)' tab's UI_MODULE_BUCKET \u2014 registering via either path now lands at the same keychain row instead of two divergent ones. v0.2.18 (Commit 3, 2026-05-18): added `openai_api_key` (scope=shared, module_id=user) so the EmbeddingService can resolve it via the hub for every project. Registration happens via `register_openai_api_key` (OnboardingWizard's OpenAI step + Preferences \u2192 Special Secrets). Validation uses the free `GET /v1/models/text-embedding-3-small` endpoint \u2014 no token consumption, no billing entry.",
  "bundled_secrets": [
    {
      "key": "github_pat",
      "scope": "shared",
      "module_id": "user",
      "description": "GitHub personal access token written by the launcher's OnboardingWizard via `register_github_pat` AND by the SecretsPanel 'Shared (this user)' tab. Resolved by the hub for every base-host project; consumed by `claude_mcp_servers/search_mcp/wrapper.sh` (renamed to GITHUB_TOKEN) and bundled hooks that need to talk to GitHub."
    },
    {
      "key": "openai_api_key",
      "scope": "shared",
      "module_id": "user",
      "description": "OpenAI API key for the EmbeddingService. Set via OnboardingWizard OR Preferences \u2192 Special Secrets. Resolved by hub for every project; consumed by vco_lib/embedding_service.py when ACTIVE_EMBEDDING=openai or as multi-slot fallback."
    }
  ]
}
